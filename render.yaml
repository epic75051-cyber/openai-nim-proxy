services:
  - type: web
    name: openai-nim-proxy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT --limit-request-line 0 --limit-request-field_size 0 app:app
    envVars:
      - key: NVIDIA_API_KEY
        sync: false
      - key: NIM_API_KEY
        sync: false
```

5. Commit

### **Or Update Your Procfile:**

If you have a `Procfile`, change it to:
```
web: gunicorn -w 4 -b 0.0.0.0:$PORT --limit-request-line 0 --limit-request-field_size 0 app:app
